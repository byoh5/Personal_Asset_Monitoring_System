# Web JSON 자산 입력기 + 엑셀 저장 기획

문서 ID: WEB-20260217-json-asset-importer-v0.2  
대상: `web_github_pages` (GitHub Pages 정적 웹 버전)  
작성일: 2026-02-17

## 1) 배경
- 현재 웹 버전은 엑셀 파일 없이 시작하면 자산 편집이 어려웠다.
- 사용자가 외부 LLM으로 만든 JSON 자산 데이터를 반영/보정하는 입력 기능이 필요하다.
- 반영한 자산 데이터를 다시 엑셀로 저장(다운로드)할 수 있어야 한다.

## 2) 목표
- 자산 페이지에 독립 `JSON 입력기` 섹션을 추가한다.
- 엑셀 미로딩 상태에서도 자산을 직접 추가/삭제할 수 있어야 한다.
- JSON 미리보기 단계에서 행 단위 수정(자산명/타입/평가금액/통화)이 가능해야 한다.
- 반영된 데이터를 브라우저에서 엑셀 파일(`Assets`, `Dashboard` 시트)로 저장할 수 있어야 한다.

## 3) 범위
- 포함
  - 직접 자산 입력(필수: 자산명/타입/평가금액) 및 목록 삭제
  - JSON 파싱(코드펜스/스마트 따옴표 보정)
  - 미리보기/검증/인라인 수정
  - 기존 자산 목록으로 upsert 반영
  - 엑셀 다운로드 저장
- 제외
  - 서버 API 연동
  - DB 저장
  - 이벤트/분개 처리

## 4) UI 설계
- 위치: 자산 페이지 내 `자산 목록` 카드 하단의 독립 섹션
- 구성
  - 직접 자산 입력 폼(`자산 추가`)
  - 자산 목록 행 단위 `삭제` 버튼
  - 외부 LLM용 프롬프트 표시 + 복사 버튼
  - JSON 입력 textarea
  - `미리보기`, `자산 반영` 버튼
  - 미리보기 결과표(상태/메시지 + 수정 가능한 셀)
  - `엑셀 저장` 버튼(자산 목록 합계 영역 옆)

## 5) 데이터 규칙
- 최소 필수: `name`, `type`, `valuation`
- 타입 허용값: `stock|etf|deposit|cash|gold|real_estate|crypto|bond|fund|pension|insurance|liability|other`
- 타입 미일치: `other`로 폴백
- `valuation`
  - 비부채 타입: `> 0`
  - 부채 타입: 음수/양수 입력 허용, 내부 저장 시 부채는 음수 valuation으로 정규화
- 스마트 따옴표(`“ ”`, `‘ ’`)는 ASCII 따옴표로 보정 후 파싱

## 6) 반영(upsert) 기준
- 기존 자산 매칭 우선순위
  1. `name + type + ticker`
  2. `name + type`
- 매칭 시 해당 자산 값을 업데이트
- 미매칭 시 신규 자산 추가
- 반영 시 `valueInput`, `valuationDisplay`, `valuation` 동기화

## 7) 엑셀 저장 방식
- 브라우저에서 새 워크북 생성 후 다운로드
- `Assets` 시트
  - 표준 헤더: 자산ID/자산명/자산타입/통화/원금평가금액/평가금액KRW/금리연/만기일/소유자/티커/시장/수량/현재가1주당수동/평균단가/은행명/계좌번호
- `Dashboard` 시트
  - 총자산/총부채/순자산
  - 자산타입별 valuation 집계
- 파일명: `asset_monitoring_web_YYYYMMDD_HHMMSS.xlsx`

## 8) 수용 기준(AC)
- JSON 입력 후 미리보기에서 오류/유효 상태가 구분되어 표시된다.
- 엑셀 미로딩 상태에서 직접 자산 추가/삭제가 가능하다.
- 사용자가 미리보기 셀을 수정하면 즉시 재검증된다.
- `자산 반영` 시 유효 행만 자산 목록에 반영된다.
- 반영 후 대시보드 합계/비중이 즉시 재계산된다.
- `엑셀 저장` 클릭 시 수정 결과가 담긴 파일이 다운로드된다.
