# GitHub Pages용 Web Viewer 구현 방안

## 1. 목표
- 기존 프로젝트는 변경하지 않고, 별도 폴더(`web_github_pages`)에 정적 웹 버전을 구현한다.
- 로그인/설정/분석/업데이트/감사로그 기능은 제거한다.
- 사용자가 기존 프로젝트에서 `Export`한 엑셀 파일을 업로드하면, `대시보드`와 `자산` 화면만 조회 가능하게 만든다.
- GitHub Pages에서 바로 배포 가능한 순수 정적 파일(`HTML/CSS/JS`) 구조로 만든다.

## 2. 범위
- 포함
- 상단 헤더 + 좌측 사이드바 레이아웃(기존 UI 스타일 최대한 유사)
- 메뉴 2개: `대시보드`, `자산`
- 엑셀 파일 선택/로딩 UI
- Dashboard 지표: `총자산`, `총부채`, `순자산`
- Dashboard 차트: `자산 타입 비중(Pie)`
- Assets 테이블: 자산명/자산타입/평가금액/통화/보조정보
- 다크/라이트 테마 토글(기존 스타일 활용)

- 제외
- 로그인/권한/사용자 관리
- 자산 생성/수정/삭제
- 이벤트/분개/감사로그
- 서버 API 호출
- DB 저장

## 3. 기술 설계
- 런타임: 브라우저(정적 페이지)
- 라이브러리(CDN)
- `Chart.js` (대시보드 Pie 차트)
- `SheetJS/xlsx` (엑셀 파싱)
- 데이터 저장: 메모리(세션)
- 선택 파일 정보(파일명/로드 시간)는 `localStorage`에 저장하여 UX 보조

## 4. 엑셀 파싱 규칙
- 우선 대상: 기존 시스템 `export_asset_excel` 결과 파일
- 시트 우선순위
- `Assets` 시트: 자산 목록 원본
- `Dashboard` 시트: 요약 수치(`B5/B6/B7`) 및 타입별 비중 테이블(`A10:B...`)

- 헤더 매핑(Assets)
- `자산ID`, `자산명`, `자산타입`, `통화`, `원금/평가금액`, `평가금액(KRW)`, `소유자(명의)`, `티커`, `시장`, `수량`, `현재가(1주당, 수동)`, `평균단가`, `은행명`, `계좌번호`
- 일부 헤더 변형(공백/영문 헤더)도 alias로 허용

- 평가금액 계산
- 1순위: `평가금액(KRW)` 값 사용
- 2순위: 주식/ETF/가상자산에서 `수량 * 현재가`
- 3순위: `원금/평가금액`
- 부채(`liability`)는 대시보드 계산 시 음수 취급하여 `총부채`로 집계

## 5. UI 재현 전략
- 기존 `blackbox_core/webui/static/css/style.css`를 복제해 스타일 최대한 동일화
- 새 앱 마크업을 기존 클래스(`app-layout`, `app-header`, `sidebar-menu`, `metrics-grid`, `audit-table` 등)에 맞춰 구성
- 기존과 동일하게 완전 재현이 어려운 부분은 아래 기본안으로 처리
- `대시보드 추이(trend)`와 `최근 로그`는 엑셀 단일 스냅샷으로는 데이터 부재이므로 표시하지 않음

## 6. 파일 구조
- `web_github_pages/index.html`
- `web_github_pages/IMPLEMENTATION_PLAN.md`
- `web_github_pages/README.md`
- `web_github_pages/assets/style.css`
- `web_github_pages/assets/app.js`

## 7. 구현 순서
1. 폴더/문서/기본 엔트리 파일 생성
2. 공통 레이아웃(헤더, 메뉴, 테마, 파일 로더) 구현
3. 엑셀 파서 구현(시트 탐색, 헤더 alias, 숫자 정규화)
4. 대시보드 렌더링(요약 카드 + Pie)
5. 자산 페이지 렌더링(테이블 + 합계)
6. 에러/빈 상태 처리
7. GitHub Pages 배포용 README 작성

## 8. 협의 필요 항목(진행 전 기본값 지정)
- 항목: 대시보드의 `추이`/`최근 로그` 영역
- 기본안: 숨김 처리(현재 구현 기준)
- 대안: 빈 상태 카드로 자리만 유지

- 항목: 기존 다국어(i18n)
- 기본안: 한국어 고정
- 대안: 추후 로케일 JSON 연동

위 기본안으로 먼저 구현 진행하고, 이후 원하시면 대안으로 조정한다.
